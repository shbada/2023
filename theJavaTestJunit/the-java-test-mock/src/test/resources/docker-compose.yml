version: "3"

services:
  study-db: # 서비스이름
    image: postgres
    ports:
      - 5432 # 랜덤 포트로 매핑됨
    environment:
      POSTGRES_PASSWORD: study
      POSTGRES_USER: study
      POSTGRES_DB: study
